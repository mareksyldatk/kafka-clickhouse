{
  "${KAFKA_BROKER_SASL_USERNAME}": "${KAFKA_BROKER_SASL_PASSWORD}",
  "${SCHEMA_REGISTRY_SASL_USERNAME}": "${SCHEMA_REGISTRY_SASL_PASSWORD}",
  "${KAFKA_CLIENT_SASL_USERNAME}": "${KAFKA_CLIENT_SASL_PASSWORD}"
}
